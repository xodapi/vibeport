export default function DashboardPage() {
  return (
    <section className="card">
      <h2 className="text-xl font-semibold">Your LLM operations, in one place</h2>
      <p className="mt-2 text-sm text-text-muted">
        Live proxy metrics will appear here in the next implementation step.
      </p>
    </section>
  );
}
